<h1>Host A Game</h1>
<%= form_with url: games_path, model: Game do |f| %>
	<div>
		<%= f.time_field :time %>
		<%= f.date_field :date %>
		<%= f.text_field :field_address, :placeholder => "Field Address" %>
		<%= f.submit "Post This Game" %>
	</div>
<% end %>